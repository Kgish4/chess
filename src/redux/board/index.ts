// import { createSlice, PayloadAction } from "@reduxjs/toolkit";
// import { BoardStore, Cell } from "../types";

// const initialState: BoardStore = {
//   activeCells: [],
// };

// const boardSlice = createSlice({
//   name: "board",
//   initialState,
//   reducers: {
//     addActiveCells(state, action: PayloadAction<Cell[]>) {
//       state.activeCells = action.payload;
//     },
//     clearActiveCells(state, action) {
//       state.activeCells = [];
//     },
//   },
// });

// export const { addActiveCells, clearActiveCells } = boardSlice.actions;
// export default boardSlice.reducer;
